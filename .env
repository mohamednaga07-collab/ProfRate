### Database - required for Drizzle, sessions, and storage
# Local Postgres via docker-compose (see docker-compose.yml)
# Start with: docker-compose up -d
# DATABASE_URL=postgresql://dbuser:dbpass@localhost:5432/campus_ratings

### Replit OIDC client id (used by `server/replitAuth.ts`)
# Obtain this from your Replit / OIDC provider
REPL_ID=your_repl_client_id

### Optional: override OIDC issuer (defaults to https://replit.com/oidc)
ISSUER_URL=https://replit.com/oidc

### Session secret - must be a long random string for production
SESSION_SECRET=change_this_to_a_strong_secret

### Port the server should bind to (default 5000)
PORT=5000

### Node environment (development|production)
NODE_ENV=development

### Email Configuration (for forgot password / username recovery)
# Gmail: Generate app password at https://myaccount.google.com/apppasswords
# 1. Enable 2-Factor Authentication on your Gmail account
# 2. Go to https://myaccount.google.com/apppasswords
# 3. Generate an app password and set EMAIL_PASSWORD below
# For other email providers, adjust the service in server/email.ts
EMAIL_USER=mohamednaga07@gmail.com
EMAIL_PASSWORD=ahxfobrqbezgbzyg
EMAIL_FROM=Campus Ratings <noreply@campusratings.com>

### Optional: App URL for password reset links (defaults to http://localhost:5173)
APP_URL=http://localhost:5000

### Google reCAPTCHA (v2 checkbox)
RECAPTCHA_ENABLED=true
VITE_RECAPTCHA_ENABLED=true

# Real reCAPTCHA keys (no more test warning!)
RECAPTCHA_SECRET_KEY=6Ld0mz4sAAAAAKVN9vSz_vYK7jdQ2UGbSS4CPYto
# IMPORTANT: You must also set VITE_RECAPTCHA_SITE_KEY for the client to work with this secret!
VITE_RECAPTCHA_SITE_KEY=6LeIxAcTAAAAAJcZVRqyHh71UMIEGNQ_MXjiZKhI