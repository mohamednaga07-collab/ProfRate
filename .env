### Database - required for Drizzle, sessions, and storage
# Local Postgres via docker-compose (see docker-compose.yml)
# Start with: docker-compose up -d
DATABASE_URL=postgresql://dbuser:dbpass@localhost:5432/campus_ratings

### Replit OIDC client id (used by `server/replitAuth.ts`)
# Obtain this from your Replit / OIDC provider
REPL_ID=your_repl_client_id

### Optional: override OIDC issuer (defaults to https://replit.com/oidc)
ISSUER_URL=https://replit.com/oidc

### Session secret - must be a long random string for production
SESSION_SECRET=change_this_to_a_strong_secret

### Port the server should bind to (default 5000)
PORT=5000

### Node environment (development|production)
NODE_ENV=development